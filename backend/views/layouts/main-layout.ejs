<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Blablalink NIKKE</title>

  <!-- Bootstrap 5 -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    crossorigin="anonymous"
  />

  <!-- Custom Styles -->
  <style>
    html, body {
      height: 100%;
    }

    body {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      background: url("/images/nikke-bg.jpg") no-repeat center center fixed;
      background-size: cover;
      color: white;
      font-family: 'Segoe UI', sans-serif;
    }

    main {
      flex: 1;
      background: rgba(0, 0, 0, 0.5);
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.4);
    }

    .card, .list-group-item {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      color: #fff;
    }

    footer {
      background: rgba(0,0,0,0.4) !important;
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
      color: white;
      text-align: center;
      padding: 20px 0;
    }

    .nikke-card {
      position: relative;
      overflow: hidden;
      border-radius: 12px;
      background: rgba(0,0,0,0.6);
      transition: transform 0.25s ease, box-shadow 0.25s ease;
      cursor: pointer;
      height: 100%;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .nikke-card img {
      width: 100%;
      object-fit: cover;
      border-radius: 12px 12px 0 0;
      flex-grow: 1;
    }

    .nikke-card.SSR {
      border: 3px solid gold;
      box-shadow: 0 0 15px rgba(255, 215, 0, 0.6);
    }

    .nikke-card.SR {
      border: 3px solid #9b59b6; /* ungu */
      box-shadow: 0 0 15px rgba(155, 89, 182, 0.6);
    }

    .nikke-card.R {
      border: 3px solid #3498db; /* biru */
      box-shadow: 0 0 15px rgba(52, 152, 219, 0.6);
    }

    /* Badge Role */
    .role-badge {
      position: absolute;
      top: 8px;
      left: 8px;
      padding: 4px 8px;
      font-size: 0.7rem;
      font-weight: bold;
      border-radius: 6px;
      color: #fff;
      text-shadow: 0 1px 2px rgba(0,0,0,0.7);
    }

    /* Warna role */
    .role-badge.Attacker {
      background: #e74c3c; /* merah */
    }

    .role-badge.Defender {
      background: #2980b9; /* biru */
    }

    .role-badge.Supporter {
      background: #27ae60; /* hijau */
    }

    /* Badge rarity */
    .rarity-badge {
      position: absolute;
      top: 8px;
      right: 8px;
      padding: 4px 8px;
      font-size: 0.75rem;
      font-weight: bold;
      color: #fff;
      border-radius: 6px;
      text-shadow: 0 1px 2px rgba(0,0,0,0.7);
    }

    /* Warna sesuai rarity */
    .rarity-badge.SSR {
      background: gold;
      color: black;
    }

    .rarity-badge.SR {
      background: #9b59b6;
    }

    .rarity-badge.R {
      background: #3498db;
    }

    .nikke-name {
      text-align: center;
      padding: 8px;
      background: rgba(0,0,0,0.75);
      color: #fff;
      font-weight: bold;
      border-top: 1px solid rgba(255,255,255,0.1);
    }

    .nikke-card:hover {
      transform: translateY(-6px) scale(1.03);
      box-shadow: 0 10px 20px rgba(0,0,0,0.6),
                  0 0 15px rgba(0,200,255,0.6);
    }

    /* Badge Burst */
    .burst-badge {
      position: absolute;
      top: 38px; /* tepat di bawah rarity badge */
      right: 8px;
      padding: 3px 6px;
      font-size: 0.7rem;
      font-weight: bold;
      border-radius: 6px;
      color: #fff;
      text-shadow: 0 1px 2px rgba(0,0,0,0.7);
    }

    /* Warna burst */
    .burst-badge.I {
      background: #1abc9c; /* teal */
    }

    .burst-badge.II {
      background: #f39c12; /* oranye */
    }

    .burst-badge.III {
      background: #c0392b; /* merah tua */
    }

    /* Filter buttons */
    .nikke-info {
      margin-top: 4px;
    }
    .nikke-info .badge {
      font-size: 0.7rem;
      margin-right: 2px;
    }

    button.active {
      background-color: #007bff;
      color: white;
      box-shadow: inset 2px 2px 5px rgba(0,0,0,0.3);
      transform: translateY(2px);
    }
    
    /* Tampilkan 10 kartu per baris di layar besar */
    @media (min-width: 1400px) {
      .nikke-card {
        width: 60%;
        margin: 0 auto;
      }

      .row > .col {
        padding-left: 0;
        padding-right: 0;
      }
    }
    /* Kotak putih blur */
  .box-blur {
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border-radius: 10px;
  padding: 15px 25px;
  max-width: 700px;
  margin: 20px auto;
  color: white;
}

.box-blur a {
  color: white !important;
  text-decoration: underline;
  font-weight: 600;
}

  /* List styling */
  ul {
    list-style: disc inside;
    padding-left: 0;
  }

  li {
    margin-bottom: 12px;
    font-size: 1.1rem;
  }

  /* Animasi fade-in */
    .custom-alert {
      opacity: 0;
      transform: translateY(-10px);
      transition: opacity 0.6s ease, transform 0.6s ease;
    }
    .custom-alert.show {
      opacity: 1;
      transform: translateY(0);
    }

  </style>
</head>
<body>
  
  <!-- Navbar -->
  <%- include('./navbar') %>

  <!-- Flash Messages -->
  <div class="container mt-3">
    <% if (success && success.length > 0) { %>
      <% success.forEach(msg => { %>
        <div class="alert alert-success alert-dismissible fade show text-center custom-alert" role="alert">
          <%= msg %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% }) %>
    <% } %>

    <% if (error && error.length > 0) { %>
      <% error.forEach(msg => { %>
        <div class="alert alert-danger alert-dismissible fade show text-center custom-alert" role="alert">
          <%= msg %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% }) %>
    <% } %>
  </div>

  <!-- Main Content -->
  <main class="container my-4">
    <%- body %>
  </main>

  <!-- Footer -->
  <%- include('./footer') %>

  <!-- Bootstrap JS -->
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    crossorigin="anonymous"
  ></script>
</body>
</html>